document.getElementById('swimmingForm').addEventListener('submit', function(e) {
    e.preventDefault();

    // Retrieve user input
    var gender = document.getElementById('gender').value;
    var event = document.getElementById('event').value;
    var time = document.getElementById('time').value;

    // TODO: Process the input, perform regression, and update the graph
    // This part requires additional logic and possibly a data visualization library
});

// TODO: Populate the events dropdown based on gender
